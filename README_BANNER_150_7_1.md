# BƯỚC 150.7.1 – BANNER TINH GỌN, CÂN ĐỐI

- Chỉ chỉnh module banner từ bản 150.7.
- Bỏ toàn bộ fade-zoom/slide/reveal/glow gây cảm giác thô.
- Dùng chuyển cảnh crossfade nhẹ 1.15 giây, không phóng to/thu nhỏ ảnh.
- Bỏ dòng nhãn phụ (số thứ tự + tên chủ đề) đè trên banner.
- Không dùng backdrop blur; ảnh banner hiển thị nguyên tỉ lệ, cân đối.
- Banner vẫn độc lập với Thư viện ảnh.
- Không thay đổi Supabase/Excel/VNEDU/vòng quay/các module quản lý.
